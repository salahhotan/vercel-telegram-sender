{
  "name": "vercel-telegram-sender",
  "version": "1.0.0",
  "description": "Vercel serverless function to send messages to a Telegram channel and store signals in Vercel KV/Edge Config.",
  "main": "api/send-message.js",
  "type": "module",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "vercel",
    "serverless",
    "telegram",
    "kv",
    "edge-config",
    "trading",
    "bot",
    "nodejs"
  ],
  "author": "Your Name <youremail@example.com>",
  "license": "ISC",
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "@vercel/edge-config": "^1.0.0",
    "@vercel/kv": "^0.0.0",
    "node-fetch": "^3.3.1"
  },
  "devDependencies": {
    "vercel": "^32.5.1"
  }
}
