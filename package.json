{
  "name": "vercel-telegram-sender",
  "version": "1.0.0",
  "description": "Vercel serverless function to send messages to a Telegram channel and store signals in Firebase.",
  "main": "api/send-message.js",
  "type": "module",
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel --prod",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "vercel",
    "serverless",
    "telegram",
    "firebase",
    "firestore",
    "trading",
    "bot",
    "nodejs"
  ],
  "author": "Your Name <youremail@example.com>",
  "license": "ISC",
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "@firebase/app": "^0.9.13",
    "@firebase/firestore": "^3.4.13",
    "firebase": "^10.7.0",
    "node-fetch": "^3.3.1"
  },
  "devDependencies": {
    "vercel": "^32.5.1"
  }
}
